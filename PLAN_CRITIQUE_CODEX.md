## Verdict: REWORK

## File:line-anchored findings
- Severity: High. Location: `IMPLEMENTATION_PLAN.md:50,108`, `src/mindroom/api/credentials.py:27-35`, `src/mindroom/tool_system/worker_routing.py:360-362`. What's wrong: the runtime facade export list omits `service_uses_local_shared_credentials`, but the plan explicitly lists `src/mindroom/api/credentials.py` as a caller to migrate from `worker_routing`, and that file imports the symbol today. Following the plan as written leaves one live cross-domain caller without a target symbol. What to do: add `service_uses_local_shared_credentials` to the `mindroom.tool_system.runtime` facade, or explicitly keep `api/credentials.py` on a named Tach exception and say so in the plan.
- Severity: High. Location: `IMPLEMENTATION_PLAN.md:81-87,173-179`, `src/mindroom/tool_system/plugins.py:106-127,463-466,486,539-541`, `src/mindroom/tool_system/metadata.py:90-100,126-152,155-164,920-950`. What's wrong: the plan says the only catalog/extensions private coupling worth keeping is `plugins._MODULE_IMPORT_CACHE`, but `plugins.py` still imports a larger set of live catalog internals: `synchronize_plugin_tools`, `_capture_tool_registry_snapshot`, `_restore_tool_registry_snapshot`, `locked_tool_registry_state`, `clear_plugin_tool_registrations`, `snapshot_plugin_tool_registrations`, `restore_plugin_tool_registrations`, and `_scoped_plugin_registration_owner`. With the proposed exclusive implementation-module interfaces, these imports will still violate the boundary unless they are exported or explicitly exempted. What to do: add a concrete disposition for each of these production couplings now: facade export, helper relocation, or named Tach exception.
- Severity: Medium. Location: `IMPLEMENTATION_PLAN.md:39-40,95-96,183-185`, `src/mindroom/tools/agentql.py:7`, `src/mindroom/tools/compact_context.py:9`, `src/mindroom/tools/browser.py:7`, `src/mindroom/tools/coding.py:7`, `tests/test_config_manager_consolidated.py:20`, `tests/test_tools_metadata.py:22,39`, `src/mindroom/tool_system/tool_hooks.py:34`, `tests/test_tool_failures.py:13`, `tests/test_sandbox_proxy.py:1052`. What's wrong: the proposed facades are materially wider than the actual cross-domain seam. Examples: `register_tool_with_metadata`, `register_builtin_tool_metadata`, `ToolManagedInitArg`, and `ToolExecutionTarget` are only used by in-domain tool-author modules that the plan itself says will stay on `metadata.py`; `AUTHORED_OVERRIDE_INHERIT` and `validate_authored_overrides` are test-only; `build_tool_failure_record`, `record_tool_failure`, and `SandboxProxyConfig` are internal/test-only. That contradicts the plan's own "prefer Tach exceptions over widening the seam for tests/private internals" rule. What to do: trim the export lists to symbols with real cross-domain callers, and leave test-only or internal-only names behind targeted exceptions.
- Severity: Medium. Location: `IMPLEMENTATION_PLAN.md:118-120`, `src/mindroom/api/sandbox_worker_prep.py:16,163`. What's wrong: this file does not import `mindroom.tool_system.sandbox_proxy` today; it already uses the package-module path `from mindroom.tool_system import sandbox_proxy` and then calls `sandbox_proxy.sandbox_proxy_config(...)`. The migration list is therefore inaccurate as written. What to do: either remove `sandbox_worker_prep.py` from the direct `sandbox_proxy` caller list, or note separately that it is already on the namespace/module-object path and only needs a future alias adjustment if desired.

## Items I checked and confirmed correct
- Every symbol named in the catalog/runtime/extensions export catalogs exists in the source file the plan names. I found no misspellings there.
- The special-tool dedup proposal has sufficient inputs for both duplication sites if implemented as described: both sites already have `agent_name`, `config`, and `delegation_depth`, and the `dynamic_tools` behavior split is represented by `enable_dynamic_tools_manager` (`src/mindroom/tool_system/dynamic_toolkits.py:226-256`, `src/mindroom/agents.py:543-570`).
- The Agno monkey-patch isolation claim checks out. I found no references to `FunctionCall._build_nested_execution_chain_async`, `_ORIGINAL_BUILD_NESTED_EXECUTION_CHAIN_ASYNC`, or `_AGNO_ASYNC_TOOL_HOOK_CHAIN_PATCHED` outside `src/mindroom/tool_system/tool_hooks.py:56-57,248-269`.
- The `emit_custom_event()` probe outcome is correct on the live tree: `_bind_hook_context()` overwrites `plugin_name` and `settings` before callback execution (`src/mindroom/hooks/execution.py:174-192`), while `CustomEventContext` preserves emitter identity separately as `source_plugin` (`src/mindroom/hooks/context.py:512-520`, `src/mindroom/tool_system/runtime_context.py:533-546`).
- The orphan assignments are reasonable. `dependencies.py` has live runtime/operational callers (`src/mindroom/api/google_integration.py:30`, `src/mindroom/api/main.py:55`, `src/mindroom/custom_tools/_google_oauth.py:8`, `src/mindroom/embeddings.py:12`), and `validate_plugin_name` is genuinely shared by plugin loading plus plugin-emitted/runtime hook paths (`src/mindroom/tool_system/plugins.py:18`, `src/mindroom/tool_system/runtime_context.py:24`, `src/mindroom/hooks/context.py:10`).
- I did not find a Phase-A-listed cross-domain caller silently dropped from the migration list. The main live-tree misses are the omitted `service_uses_local_shared_credentials` symbol and the `sandbox_worker_prep.py` misclassification above.

## Out-of-scope reviewer temptations I am NOT raising
- I am not asking to split `metadata.py`, `plugins.py`, `worker_routing.py`, `runtime_context.py`, or `sandbox_proxy.py` into smaller files.
- I am not asking for speculative hardening around plugin reload races, hook dispatch edge cases, or the Agno monkey-patch beyond the scoped isolation/comment requirement already in the prompt.
- I am not asking to redesign `_TOOL_REGISTRY`, MCP registry ownership, or the removed approval subsystem. Those are separate issues unless a real regression appears in this seam work.
